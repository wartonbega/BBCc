struct Toto {
    Int a,
    Int b,

    func test () Toto {
        
    }
}

func test(Bool abcd) String {
    let b = 2
    let af = b + abcd
    "zefzef"
}

func fibRec2(Int x) Int {
    let ret = 
        if x == 0 0
        elif x == 1 1
        else fibRec2(x - 1) + fibRec2(x - 2)
    ret
}



func fib(Int x) Int {
    let a = 0
    let b = 1
    while x > 0 { 
        let c = a + b
        b = a
        a = c
        x = x - 1
    }
    a
}


func main() Int {
    println(fibRec2(10))
    println(fib(10))
    0
}
